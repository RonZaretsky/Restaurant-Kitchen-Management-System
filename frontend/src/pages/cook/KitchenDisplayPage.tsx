import Typography from "@mui/material/Typography";

/**
 * Placeholder for the Kitchen Display surface. Real content ships in Epic 5.
 *
 * @returns The surface's heading, standing in until its own story ships.
 */
export function KitchenDisplayPage() {
  return <Typography variant="h5" component="h1">Kitchen Display</Typography>;
}
