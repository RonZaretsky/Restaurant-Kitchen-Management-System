import Typography from "@mui/material/Typography";

/** Placeholder for the Dishes (view-only) surface. Real content ships in Epic 2. */
export function DishesPage() {
  return <Typography variant="h5">Dishes</Typography>;
}
