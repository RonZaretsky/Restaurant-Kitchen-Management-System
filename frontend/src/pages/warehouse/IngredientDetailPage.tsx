import Typography from "@mui/material/Typography";

/**
 * Placeholder for the Ingredient detail surface. Real content ships in Epic 4.
 *
 * @returns The surface's heading, standing in until its own story ships.
 */
export function IngredientDetailPage() {
  return <Typography variant="h5" component="h1">Ingredient detail</Typography>;
}
