import Typography from "@mui/material/Typography";

/**
 * Placeholder for the Table/Order detail surface. Real content ships in Epic 3.
 *
 * @returns The surface's heading, standing in until its own story ships.
 */
export function TableOrderDetailPage() {
  return <Typography variant="h5" component="h1">Table/Order detail</Typography>;
}
