import Typography from "@mui/material/Typography";

/**
 * Placeholder for the Recipe Suggestions surface. Real content ships in Epic 6.
 *
 * @returns The surface's heading, standing in until its own story ships.
 */
export function RecipeSuggestionsPage() {
  return <Typography variant="h5" component="h1">Recipe Suggestions</Typography>;
}
