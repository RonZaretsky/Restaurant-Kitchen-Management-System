import Typography from "@mui/material/Typography";

/**
 * Placeholder for the Menu Management surface. Real content ships in Epic 2.
 *
 * @returns The surface's heading, standing in until its own story ships.
 */
export function MenuManagementPage() {
  return <Typography variant="h5" component="h1">Menu Management</Typography>;
}
