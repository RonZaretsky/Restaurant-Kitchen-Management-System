import Typography from "@mui/material/Typography";

/**
 * Placeholder for the Tables setup surface. Real content ships in Epic 2.
 *
 * @returns The surface's heading, standing in until its own story ships.
 */
export function TablesSetupPage() {
  return <Typography variant="h5" component="h1">Tables setup</Typography>;
}
