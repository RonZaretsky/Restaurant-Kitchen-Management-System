import Typography from "@mui/material/Typography";

/**
 * Placeholder for the Users surface. Real content ships in a later follow-up story (see deferred-work.md).
 *
 * @returns The surface's heading, standing in until its own story ships.
 */
export function UsersPage() {
  return <Typography variant="h5" component="h1">Users</Typography>;
}
