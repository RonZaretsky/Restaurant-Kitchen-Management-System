---
scope: Full-system architecture spine for the Restaurant Kitchen Management System: FastAPI+dependency-injector backend (brownfield scaffold) and React+Vite frontend (near-greenfield), covering all 24 FRs / 6 features from the finalized 2026-07-24 PRD
purpose: Build substrate to unblock epics/stories and coding; resolve real-time mechanism, auth/session, migrations, and frontend stack before implementation starts
altitude: feature (keeps epics — bmad-create-epics-and-stories is the next step downstream)
updated: 2026-07-30T22:08
---

- (decision) Real-time kitchen/order updates delivered over WebSockets (FastAPI native support); REST stays the write path, WS is push-only for board refresh
- (decision) Auth via JWT access token delivered as an httpOnly cookie; stateless per-request verification, no server-side session store
- (decision) Adopt Alembic for DB schema migrations, replacing the current Base.metadata.create_all-on-startup pattern; use Alembic's async template since the engine is asyncpg-based
- (decision) Frontend routing: React Router v7 (SPA/data mode; no SSR needed, backend is a separate FastAPI service)
- (decision) UI component library: MUI (Material UI) -- React 19 compatible; DataGrid/table/form components map directly to order/kitchen/inventory/admin screens
- (decision) Frontend state: TanStack Query v5 owns all server-derived state (orders, tables, stock, dishes, users), cache updated by WebSocket pushes; plain React state/context for local-only UI state; no Redux/Zustand
- (decision) This architecture pass produces the build spine only (Fast path, Ofek's established preference); a fuller human-facing OOD design document is a later pass, closer to submission
- (constraint) [ADOPTED from PRD NFR-6] Concurrent Table/Order edits resolve last-write-wins -- no optimistic locking or conflict UI in v1
- (constraint) [ADOPTED from PRD NFR-3] Stock deduction when an Order Item transitions to in_preparation must be atomic (single DB transaction) -- a stricter guarantee than NFR-6's last-write-wins, must not be weakened by it
- (constraint) [ADOPTED from PRD FR-5/FR-8] Order Item stores price_at_add at creation time; Order totals always compute from stored per-item prices, never the Dish's live price
- (constraint) [ADOPTED from PRD FR-22] A Dish cannot be marked available/live while its Recipe has zero ingredient lines
- (constraint) [ADOPTED from PRD FR-2/FR-6] Permission model is role-level only -- all Waiters see all Tables/Orders, no per-resource/per-user filtering anywhere in the system
- (constraint) [ADOPTED from PRD FR-19] A published Dish's Recipe keeps a nullable provenance link back to the AI Recipe Suggestion it originated from, when applicable
- (constraint) [ADOPTED from PRD FR-20] Smart Chef chat sessions/suggestions are shared (role-level permission, not per-Cook); default list view filters to the current Cook as a personalization default only, not an access boundary
- (constraint) [RATIFIED, brownfield] dependency-injector DeclarativeContainer (backend/container.py) is the composition root; any new lifecycle-managed resource (DB engine, future OpenAI client, WS connection registry if resource-like) must be wired as a providers.Resource following the existing logging/database init/teardown pattern, never instantiated ad hoc in route handlers or services
- (constraint) [RATIFIED, brownfield] Backend layered folders (api/, clients/, data_models/, exceptions/, services/) are the only top-level organizing unit; new domain code (orders, kitchen, inventory, smart-chef, admin) fits into these by responsibility, no new top-level backend modules
- (constraint) [RATIFIED, brownfield] Backend routing starts as one APIRouter (api/router.py); convention is a dedicated sub-router per domain, included into the main router, once a second domain's endpoints are added
- (constraint) [RATIFIED, brownfield] Frontend folders (pages/, components/, services/, types/, config/) are the only top-level src/ organizing unit; env access always goes through src/config/config.ts, never raw import.meta.env in components
- (constraint) [GAP, must add] No CORS middleware exists yet; must be added to the FastAPI app before any real browser-origin frontend call (frontend :3000/:80, backend :8000 are different origins)
- (constraint) [GAP, must add] No auth/authz layer exists in code yet despite User.role enum already defined in the schema; every route is currently unauthenticated
- (version) React Router v7.8.0 (latest stable, verified 2026-07-30)
- (version) MUI (Material UI), current major -- confirmed React 19 compatible (verified 2026-07-30); Mantine considered as the alternative, also React 19 clean, not chosen
- (version) TanStack Query v5 (current major, verified 2026-07-30)
- (version) openai python SDK v2.48.0 (current, verified 2026-07-30) for the Smart Chef integration
- (version) Alembic 1.18.5 (current stable, verified 2026-07-30) -- must use its async template (alembic init -t async) since the engine is asyncpg-based; the default sync template's env.py does not work with async drivers
- (version) Existing pinned floors unchanged (backend/pyproject.toml): fastapi>=0.115.0, dependency-injector>=4.41.0, sqlalchemy[asyncio]>=2.0.0, asyncpg>=0.29.0; Postgres 16-alpine (docker-compose.yml)
- (assumption) [ASSUMPTION] OpenAI client wrapped behind an interface in backend/clients/ (matching the existing clients/database.py pattern), not called directly from services -- keeps Smart Chef swappable/testable
- (assumption) [ASSUMPTION] JWT session duration: 8-hour access token (matches a work shift), silent re-login on expiry rather than a refresh-token flow -- resolves the PRD's open 'session-survives-refresh duration' question; flag for Ofek's review
- (assumption) [ASSUMPTION] Smart Chef uses a single configurable OpenAI chat model (env-configured), no per-feature model selection in v1
- (question) Table delete/renumber (PRD's other open question) stays out of scope for this architecture pass -- it's a product/FR-level decision, not an architectural one; deferred to epics/stories
- (decision) [ADOPTED from PRD FR-7] OrderItem cancel/void is a status transition (a new terminal status), never a row delete -- preserves audit trail; if the item had already reached in_preparation (stock deducted), cancellation must reverse the deduction via a new StockMovement, never silently lose stock
- (gap) [GAP, brownfield] Current OrderItem.status enum (pending/in_preparation/ready, per docs/database-schema.md) predates the PRD and has no cancelled/void state (FR-7) and Order lacks it too beyond the documented served/closed additions -- Alembic migration must extend both enums; exact enum literal names are left to the code (seed), the invariant (AD-11) is binding
- (direction) Backend paradigm: layered service architecture (api -> services -> clients/data_models, exceptions cross-cutting) with the existing dependency-injector container as composition root -- ratified from brownfield, not invented
- (direction) Frontend paradigm: layered SPA (pages -> components, both -> services -> types) with TanStack Query as the server-state layer -- ratified from the existing empty-but-intentional folder structure, filled in with this run's stack choices
- (gap) [CORRECTION] reconcile-brownfield review found the earlier '[RATIFIED, brownfield] backend/exceptions/ top-level' entry overstated brownfield reality: an empty stray backend/data_models/exceptions/__init__.py already exists on disk, conflicting with project-context.md's explicit rule that backend/exceptions/ (top-level) is the designated location. Decision: keep backend/exceptions/ top-level per the documented rule (architecturally cleaner -- exceptions are cross-cutting, not a data-model concern); the stray data_models/exceptions/ folder is scaffold debris to remove/relocate during implementation, not a competing convention to ratify
- (event) Reviewer Gate run: 5 parallel passes (reconcile-PRD, reconcile-brownfield, rubric walker, version-check, adversarial incompatibility hunt). Verdict: has-gaps, 1 critical (AD-11 contradicted FR-7), several high/medium findings, all fixed in this pass. Full reports in reviews/
- (decision) [FIX, CRITICAL] AD-11 rewritten: no automatic compensating StockMovement on cancelling an in_preparation item -- matches PRD FR-7/addendum verbatim ('treated as already-consumed'); WM may log a manual waste movement separately (FR-15)
- (decision) [FIX] AD-6 broadened from 'in_preparation transition only' to 'every OrderItem status transition', now requires a guarded conditional update (WHERE status = expected, rowcount-checked) to actually satisfy NFR-3 and close the AD-6xAD-11 race the incompatibility-hunt review constructed
- (decision) [FIX] AD-2 Binds broadened to include Low-Stock Alerts (FR-14/UJ-3); Rule tightened so a state change is emitted exactly once under one canonical event name, closing the two-features-name-the-same-event-differently gap
- (decision) [FIX] AD-7 now explicitly excludes cancelled/voided OrderItems from Order.total_amount
- (decision) [FIX] AD-8 now also covers emptying an already-available Dish's last RecipeIngredient row, not just the availability-on transition
- (decision) [FIX] AD-10 given a concrete data contract: AIChatSession/AIRecipeSuggestion list endpoints always return every row with an owner user_id; current-Cook-first is a client-side sort, never a server-side mine-first filter param
- (decision) [NEW AD-14, ADOPTED PRD FR-18/FR-21] Smart Chef request lifecycle integrity: in-flight generation guard per Cook (reject not queue) + write-only-after-success (no orphaned rows on AI failure)
- (decision) [NEW AD-15, ADOPTED PRD FR-3] Last-Admin lockout guard: reject any User update that would leave zero active Admins
- (decision) [NEW AD-16, ADOPTED PRD FR-15] Stock never floor-capped at zero on either the automatic or manual movement path
- (decision) [FIX] AD-4 gained a one-line rule on Alembic multi-head avoidance across parallel branches
- (decision) [FIX] Consistency Conventions: timestamps now explicitly tz-aware UTC (not just 'ISO 8601 UTC', which datetime.utcnow() naive vs aware both nominally satisfy); error envelope now acknowledges FastAPI's native validation-error list shape under 'detail', not just a plain string
- (version) [FIX] Stack table: MUI pinned explicitly to v9 (was vague 'current major')
- (gap) [FIX] Deferred section gained two housekeeping notes: the stray backend/data_models/exceptions/ folder to remove, and pnpm/TypeScript version-bump recommendations (pnpm past EOL, TS 2 majors behind) -- both ratified as-is for this sprint, not blocking
- (event) spine finalized
